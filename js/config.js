/* ===========================================================
 *  설정 파일 — 배포 시 여기만 수정하면 됩니다.
 * =========================================================== */
window.CONFIG = {
  // Google Apps Script 웹앱 배포 URL (배포 후 붙여넣기).
  // 비워두면 '로컬 전용 모드'로 동작 — 로그인/진도/시험은 기기에만 저장되고
  // 카톡 전송은 안 됩니다(앱 기능 테스트는 그대로 가능).
  APPS_SCRIPT_URL: "",

  // 카카오 JavaScript 키 (학생용 '결과 공유하기' 버튼에만 사용 — 선택).
  // 자동 카톡 전송은 Apps Script가 처리하므로 이 키가 없어도 됩니다.
  KAKAO_JS_KEY: "",

  // 영어 시험일 (D-day 카운트다운)
  EXAM_DATE: "2026-07-03",

  // 앱 제목
  APP_TITLE: "동대전고2 기말 파이널",
  APP_SUBTITLE: "2026 1학기 · S반·A반 공용",

  // 반 목록
  CLASSES: ["S반", "A반"],

  // 합격 기준(%) — 이 점수 이상이면 통과 표시
  PASS_PCT: 80
};
